<p>A quote has been added to your chain by #{{ curUser.id }} {{ curUser.firstname }} {{ curUser.lastname }} ({{ curUser.email }})</p>
<p>View your chain <a href="{{ url('chain_page', {'chainId': chain.id}) }}">{{ chain.name }}</a></p>
