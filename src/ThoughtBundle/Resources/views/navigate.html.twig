<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="/">
                {{ 'navbar.brand'|trans }}
            </a>
        </div>
        <ul class="nav navbar-nav navbar-right">
            <li>
                <a href="{{ path('content', {'code' : 'instruction'}) }}">
                    {{ 'navbar.instruction'|trans }}
                </a>
            </li>
            {% if app.user %}
                {% if is_granted('ROLE_SUPER_ADMIN') %}
                    <li>
                        <a href="{{ path('sonata_admin_dashboard') }}">
                            {{ 'navbar.super_admin'|trans }}
                        </a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('sonata_user_profile_show') }}">
                            {{ 'navbar.profile'|trans }}
                        </a>
                    </li>
                {% endif %}
                <li>
                    <a href="{{ path('fos_user_security_logout') }}">
                        {{ 'navbar.sign_out'|trans }}
                    </a>
                </li>
            {% else %}
                <li>
                    <a href="{{ path('fos_user_registration_register') }}">
                        {{ 'navbar.registration'|trans }}
                    </a>
                </li>
                <li>
                    <a href="{{ path('fos_user_security_login') }}">
                        {{ 'navbar.sign_in'|trans }}
                    </a>
                </li>
            {% endif %}
        </ul>


    </div>
</nav>