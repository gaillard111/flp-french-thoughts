{% extends 'ThoughtBundle::layout.html.twig' %}

{% block page_title %}{{ content.title }}{% endblock %}

{% block content %}
    <p>{{ content.title }}</p>
    {{ content.content|raw }}
{% endblock %}
