{% extends '@Thought/layout.html.twig' %}

{% block page_title %}Les fils de la pensée{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-sm-12">
            <h2>Problematic of the day</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-12">
            <div class="row">
                {% for topic in topics %}
                    <div class="col-sm-4 chain-list">
                        <a href="{{ path('topic_page', { 'topicId': topic.id }) }}">{{ topic.name|slice(0, 20) }}</a>
                        {% if topic.user == app.user %}
                            <div class="chain-edit">
                                <a class="text-primary plain" href="{{ path('sonata_user_topic_edit', { 'topicId': topic.id }) }}">
                                    <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                                </a>
                            </div>
                        {% endif %}
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}