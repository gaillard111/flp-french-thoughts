{% extends '@Thought/layout.html.twig' %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block page_title %}Les fils de la pensée{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-sm-12">
            <h2>{{ 'topic.title'|trans }}</h2>
                {{ form_start(form) }}
                    <div class="row">
                        <div class="col-sm-10">
                            {{ form_label(form.name) }}
                            {{ form_widget(form.name) }}
                        </div>
                        <div class="col-sm-2 margin-top-25">
                            {{ form_widget(form.submit) }}
                            <a href="{{ path('all_topics') }}" class="btn btn-warning">{{ 'thought.filter.buttons.reset'|trans }}</a>
                        </div>
                    </div>
                {{ form_end(form) }}
        </div>
    </div>
    <div class="row">
        <div class="col-sm-12">
            <div class="row">
                {% for topic in topics %}
                    <div class="col-sm-4 chain-list">
                        <a href="{{ path('topic_page', { 'topicId': topic.id }) }}" title="{{ topic.name }}">{{ topic.name|striptags|truncate(25) }}</a>
                        {% if topic.user == app.user %}
                            <div class="chain-edit">
                                <a class="text-primary plain" href="{{ path('sonata_user_topic_edit', { 'topicId': topic.id }) }}">
                                    <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                                </a>
                            </div>
                        {% endif %}
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}