User <a href="{{ url('thought_profile', { 'userId': user.id }) }}">{{ user.firstname }}</a> also left a comment to the <a href="{{ url('thought_thoughtpage_index', { 'thoughtId': thought.id }) }}">{{ thought.category }}</a> quote