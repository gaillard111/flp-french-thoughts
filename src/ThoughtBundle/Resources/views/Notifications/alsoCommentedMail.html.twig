User <a href="{{ url('thought_profile', { 'userId': user.id }) }}">{{ user.firstname }}</a> also commented this <a href="{{ url('thought_thoughtpage_index', { 'thoughtId': thought.id }) }}">{{ thought.category }}</a> quote